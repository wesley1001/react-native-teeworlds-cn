import {
  StyleSheet,
} from 'react-native';

import globalStyles from '../styles';

const localStyles = StyleSheet.create({
  title: {
  },
});
export default {
  ...globalStyles,
  ...localStyles,
};
